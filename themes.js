function themes() {};

themes.colorSchemes = {
  // add color schemes here
  // example 
  // default: {
  //    squares : ['black','white'] 
  //    pieces : ['black','white'] 
  // }
}

themes.changeTheme = function(theme) {
  // get the theme name and get colors from the theme
  // loop through the squares and set the color style
  // loop through the pieces and set the color style
}